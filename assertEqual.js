// FUNCTION IMPLEMENTATION
const assertEqual = function(actual, expected) {
    return (actual + " " + expected)
};

// TEST CODE
console.assert(assertEqual("Lighthouse Labs", "Bootcamp") == "Lighthouse Labs Bootcamp")

